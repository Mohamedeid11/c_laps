#include <stdio.h>
#include <stdlib.h>

// 2- C Program to print ASCII number of a given char
int main() {
    char character;

    printf("Enter the Character: ");

    scanf("%c", & character);

    printf("\nThe ASCII Value of %c is %d", character);

    return 0;
}
